[package]
name = "opto-sync-sqlx"
version = "0.2.0"
edition = "2021"
description = "opto-sync jsonb reconciliation helpers for sqlx workflows"

[dependencies]
serde_json = "1.0"
syncer-rs = { path = "../../../bindings/rust" }
