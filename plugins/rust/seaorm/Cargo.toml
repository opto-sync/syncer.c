[package]
name = "opto-sync-seaorm"
version = "0.2.0"
edition = "2021"
description = "opto-sync jsonb reconciliation helpers for SeaORM workflows"

[dependencies]
serde_json = "1.0"
syncer-rs = { path = "../../../bindings/rust" }
