name: differential_runner
description: Dart runner for the syncer.c cross-language differential test.
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  syncer:
    path: ../bindings/dart
